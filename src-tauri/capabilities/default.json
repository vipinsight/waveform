{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core access for the app window and the dictation overlay. Without event permissions neither window can receive pushed state, which the overlay depends on to know when to listen.",
  "windows": ["main", "overlay"],
  "permissions": ["core:default"]
}
