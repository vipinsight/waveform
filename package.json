{
  "name": "waveform",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first desktop voice transcription with selectable speech models.",
  "main": "dist/src/main/main.js",
  "packageManager": "pnpm@9.4.0",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-static.mjs",
    "dev": "pnpm build && electron .",
    "start": "pnpm build && electron .",
    "package:mac": "pnpm build && electron-packager . Waveform --platform=darwin --arch=arm64 --out=release --overwrite --app-bundle-id=com.webtiara.waveform --app-category-type=public.app-category.productivity --usage-description.Microphone='Waveform uses your microphone to transcribe speech locally.' --ignore='^/(?:\\.git|\\.venv-qwen|node_modules|release)(?:/|$)' && codesign --force --deep --sign - release/Waveform-darwin-arm64/Waveform.app",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "setup:model": "./scripts/setup-model.sh",
    "setup:qwen": "./scripts/setup-qwen.sh"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "@types/node": "^24.3.0",
    "electron": "^44.2.0",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
